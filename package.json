{
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "demo": "pnpm run build && node examples/express-server/server.js",
    "dev": "pnpm run build && node examples/express-server/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "express": "^5.2.1",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
